{
  "name": "თომთემატიკა",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": ".",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "tsc --noEmit",
    "preview": "vite preview",
    "deploy": "npm run build && cd dist && git init && git add . && git commit -m 'deploy' && git remote add origin https://github.com/lexo123/thomthematica.git && git push origin HEAD:site -f && cd .."
  },
  "dependencies": {
    "@google/genai": "^1.32.0",
    "gh-pages": "^6.3.0",
    "react": "^19.2.1",
    "react-dom": "^19.2.1"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
